- if controller_name != 'sessions'
  = link_to "Log in here", new_session_path(resource_name), class: 'btn'
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "Sign up here", new_registration_path(resource_name), class: 'btn'
  %br/
